extends ./layouts/master

block append page_title
  title= config.app.sitename

block append head_end_tag
  link(href=asset_path('css/frontend/index.css') rel='stylesheet')

block append content
  section.announcement_sec
    div.container
      div.row
        div.col
          div.announcement_block
            div.announcement_left
              span(class='span_icon'): i.fas.fa-bullhorn
            div.announcement_middle
              marquee(direction='up' scrollamount='1' height='20' onMouseOver='this.stop()' onMouseOut='this.start()')
                ul.announcement_list
                  li: a(href='#') 這是這是這是這是列表這是列表這是列表這是列表這是列表這是列表這是列表這是列表這是列表這是列表這是列表這是列表這是列表這是列表這是列表
                  li: a(href='#') 這是列表這是列表這是列表這是列表這是列表
                  li: a(href='#') 這是列表這是列表這是列表這是列表這是列表
                  li: a(href='#') 這是列表這是列表這是列表這是列表這是列表
                  li: a(href='#') 這是列表這是列表這是列表這是列表這是列表
            div.announcement_right
              a(href='#'): i.fas.fa-list-alt
  
  section.slick_slider_sec
    div.container
      div.row
        div.col
          div.slick_slider_container
            div.slick_slider
              div.slick_content
                div.inner_content
                  a(href='#' class='content_link'): img(src='//via.placeholder.com/600x300' class='content_img')
              div.slick_content
                div.inner_content
                  a(href='#' class='content_link'): img(src='//via.placeholder.com/1200x400' class='content_img')
              div.slick_content
                div.inner_content
                  a(href='#' class='content_link'): img(src='//via.placeholder.com/1200x400' class='content_img')
    
  
  //-
    p= testVar
  
  //-
    img(src=config.appenv.storage.domain + config.appenv.storage.path + '/abc.png')
  //-
    form(action='/image-upload?_csrf=' + csrfToken method='post' enctype='multipart/form-data')
      input(type='text' name='this_image_text')
      
      input(type='file' name='this_image')
      br
      i.far.fa-address-book
      input(type='submit' value='送出')

block append body_end_others
  script(src=asset_path('vendors/plugins/slick/slick.min.js'))
  script.
    $(function(){
      $('.slick_slider').slick({
        arrows: true,
        dots: true,
        infinite: true,
        speed: 300,
        slidesToShow: 1,
        slidesToScroll: 1,
        responsive: [
          {
            breakpoint: 768,
            settings: {
              arrows:false
            }
          }
        ]
      });
    });
